Exercice 15: swap_bits

Turn-in directory : ex15/
Files to turn in  : swap_bits.c
Allowed functions : None

* Write a function that takes a byte, swaps its halves (like the example)
  and returns the result.

* Here's how it should be prototyped :

    unsigned char	swap_bits(unsigned char octet);

* Example:

      1 byte
    _____________
     0100 | 0001
         \ /
         / \
     0001 | 0100

(See ../SUBJECT.txt for the level-wide instructions and submission rules.)
