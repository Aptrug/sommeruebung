# Notes on this pass

This file is not part of the exam. It's a record of the one place where I
changed *content*, not just formatting, while making these SUBJECT.txt files
consistent with c02's style. Delete this file before handing the directory
to anyone as an actual exam.

## level3/ft_range

The original `subject.txt` for `ft_range` was a verbatim copy of
`ft_rrange`'s content: same prototype (`ft_rrange`, not `ft_range`), same
"begin at end and end at start" (descending) behavior, same examples. That
looks like a copy-paste error rather than an intentional duplicate exercise,
since `ft_range` / `ft_rrange` is a standard ascending/descending pair in
this exam pool.

I rewrote `ft_range`'s SUBJECT.txt as the ascending counterpart:
`int *ft_range(int start, int end)`, filling the array from `start` up to
`end`, inclusive, with matching examples. `ft_rrange` (descending) was left
as-is.

If that's not what you wanted — e.g. if `ft_range` was meant to be something
else entirely, or you'd rather flag the duplicate and leave it broken until
you decide — let me know and I'll change it.

## Everything else

All other differences from the original text are formatting only (c02-style
bullets/headers/footer, `SUBJECT.txt` capitalization, LF line endings) or
plain typo fixes (adresses→addresses, postive→positive, multiples→multiple,
delimitated→delimited, boundry→boundary, progam→program,
additionnal→additional). No other instructions, constraints, or examples
were altered.
